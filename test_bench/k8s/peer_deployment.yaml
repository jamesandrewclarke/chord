apiVersion: apps/v1
kind: Deployment
metadata:
  name: chord-peer
spec:
  replicas: 25
  selector:
    matchLabels:
      app: chord-peer
  template:
    metadata:
      labels:
        app: chord-peer
        team: chord
    spec:
      tolerations:
      - key: "kubernetes.azure.com/scalesetpriority"
        operator: "Equal"
        value: "spot"
        effect: "NoSchedule"
      imagePullSecrets:
      - name: regcred
      containers:
      - name: chord
        image: chord.azurecr.io/app/peer:ci
        command: ["/app", "-port=8080", "-address=$(EXTERNAL_IP)", "-bootstrap=10.244.3.71:8080"]
        resources:
          requests:
            memory: "64Mi"
            cpu: "128m"
          limits:
            memory: "512Mi"
            cpu: "256m"
        ports:
        - name: rpc
          containerPort: 8080
        - name: metrics
          containerPort: 2112

        env:
        - name: EXTERNAL_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
